# Wireshark-Traffic-Analysis
A hands-on project where I used Wireshark to capture and study real network traffic on Windows — covering packet capturing basics, the TCP handshake, DNS lookups, HTTP vs HTTPS, ICMP (ping), FTP, and analyzing traffic from simulated attacks like port scans, brute-force login attempts, and SQL injection.
